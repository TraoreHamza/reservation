{% extends 'base.html.twig' %}

{% block title %}Test de recherche{% endblock %}

{% block body %}
    <h1>🧪 Test de recherche</h1>
    
    <div class="row">
        {% for room in rooms %}
            <div class="col-md-4 mb-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ room.name }}</h5>
                        <p class="card-text">
                            <strong>Capacité :</strong> {{ room.capacity }} personnes<br>
                            <strong>Disponible :</strong> 
                            {% if room.isAvailable %}
                                <span class="badge bg-success">Oui</span>
                            {% else %}
                                <span class="badge bg-danger">Non</span>
                            {% endif %}
                        </p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    
    <hr>
    
    <h2>Test du composant</h2>
    {{ component('search_room_component', { query: 'quo' }) }}
{% endblock %} 