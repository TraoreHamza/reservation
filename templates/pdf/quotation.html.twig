<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Devis {{ quotation.reference }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            line-height: 1.4;
            color: #333;
            margin: 0;
            padding: 20px;
        }
        .header {
            text-align: center;
            border-bottom: 2px solid #3498db;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        .header h1 {
            color: #2c3e50;
            margin: 0;
            font-size: 24px;
        }
        .header p {
            margin: 5px 0;
            color: #7f8c8d;
        }
        .info-section {
            margin-bottom: 25px;
        }
        .info-section h2 {
            color: #34495e;
            border-bottom: 1px solid #bdc3c7;
            padding-bottom: 5px;
            font-size: 16px;
            margin-bottom: 15px;
        }
        .info-grid {
            display: table;
            width: 100%;
            border-collapse: collapse;
        }
        .info-row {
            display: table-row;
        }
        .info-cell {
            display: table-cell;
            padding: 8px;
            border: 1px solid #ddd;
            vertical-align: top;
        }
        .info-label {
            font-weight: bold;
            background: #f8f9fa;
            width: 30%;
        }
        .info-value {
            width: 70%;
        }
        .price-section {
            background: #f8f9fa;
            padding: 15px;
            border: 1px solid #ddd;
            margin: 20px 0;
        }
        .price-table {
            width: 100%;
            border-collapse: collapse;
        }
        .price-table th,
        .price-table td {
            padding: 8px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        .price-table th {
            background: #ecf0f1;
            font-weight: bold;
        }
        .total-row {
            font-weight: bold;
            background: #ecf0f1;
        }
        .description {
            background: #fff;
            padding: 15px;
            border: 1px solid #ddd;
            margin: 20px 0;
        }
        .terms {
            background: #f8f9fa;
            padding: 15px;
            border: 1px solid #ddd;
            margin: 20px 0;
            font-size: 11px;
        }
        .footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            text-align: center;
            font-size: 10px;
            color: #7f8c8d;
        }
        .page-break {
            page-break-before: always;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>DEVIS {{ quotation.reference }}</h1>
        <p><strong>Service de Réservation de Salles</strong></p>
        <p>123 Rue de la Réservation, 75001 Paris</p>
        <p>Tél: 01 23 45 67 89 | Email: contact@reservation.com</p>
    </div>

    <div class="info-section">
        <h2>Informations du devis</h2>
        <div class="info-grid">
            <div class="info-row">
                <div class="info-cell info-label">Référence</div>
                <div class="info-cell info-value">{{ quotation.reference }}</div>
            </div>
            <div class="info-row">
                <div class="info-cell info-label">Date de création</div>
                <div class="info-cell info-value">{{ quotation.created_at|date('d/m/Y') }}</div>
            </div>
            <div class="info-row">
                <div class="info-cell info-label">Valide jusqu'au</div>
                <div class="info-cell info-value">{{ quotation.validUntil|date('d/m/Y') }}</div>
            </div>
            <div class="info-row">
                <div class="info-cell info-label">Statut</div>
                <div class="info-cell info-value">{{ quotation.statusLabel }}</div>
            </div>
        </div>
    </div>

    <div class="info-section">
        <h2>Informations client</h2>
        <div class="info-grid">
            <div class="info-row">
                <div class="info-cell info-label">Nom du client</div>
                <div class="info-cell info-value">{{ client.name }}</div>
            </div>
            <div class="info-row">
                <div class="info-cell info-label">Email</div>
                <div class="info-cell info-value">{{ user.email }}</div>
            </div>
        </div>
    </div>

    <div class="info-section">
        <h2>Détails de la réservation</h2>
        <div class="info-grid">
            <div class="info-row">
                <div class="info-cell info-label">Salle réservée</div>
                <div class="info-cell info-value">{{ room.name }}</div>
            </div>
            <div class="info-row">
                <div class="info-cell info-label">Capacité</div>
                <div class="info-cell info-value">{{ room.capacity }} personnes</div>
            </div>
            <div class="info-row">
                <div class="info-cell info-label">Date de début</div>
                <div class="info-cell info-value">{{ booking.startDate|date('d/m/Y') }}</div>
            </div>
            <div class="info-row">
                <div class="info-cell info-label">Date de fin</div>
                <div class="info-cell info-value">{{ booking.endDate|date('d/m/Y') }}</div>
            </div>
            <div class="info-row">
                <div class="info-cell info-label">Durée</div>
                <div class="info-cell info-value">{{ booking.startDate|date('d/m/Y')|date('U')|date('z') - booking.endDate|date('d/m/Y')|date('U')|date('z') }} jours</div>
            </div>
        </div>
    </div>

    {% if quotation.description %}
    <div class="description">
        <h3>Description de la prestation</h3>
        <p>{{ quotation.description|nl2br }}</p>
    </div>
    {% endif %}

    <div class="price-section">
        <h2>Détail des prix</h2>
        <table class="price-table">
            <thead>
                <tr>
                    <th>Description</th>
                    <th>Prix unitaire</th>
                    <th>Quantité</th>
                    <th>Total HT</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Location de la salle "{{ room.name }}"</td>
                    <td>{{ room.formattedPrice }}</td>
                    <td>{{ booking.startDate|date('d/m/Y')|date('U')|date('z') - booking.endDate|date('d/m/Y')|date('U')|date('z') }} jours</td>
                    <td>{{ quotation.formattedPrice }}</td>
                </tr>
                {% for option in booking.options %}
                <tr>
                    <td>Option : {{ option.name }}</td>
                    <td>{{ option.formattedPrice }}</td>
                    <td>1</td>
                    <td>{{ option.formattedPrice }}</td>
                </tr>
                {% endfor %}
                <tr class="total-row">
                    <td colspan="3">Total HT</td>
                    <td>{{ quotation.formattedPrice }}</td>
                </tr>
                <tr>
                    <td colspan="3">TVA ({{ quotation.taxRate }}%)</td>
                    <td>{{ quotation.formattedTaxAmount }}</td>
                </tr>
                <tr class="total-row">
                    <td colspan="3"><strong>Total TTC</strong></td>
                    <td><strong>{{ quotation.formattedTotalPrice }}</strong></td>
                </tr>
            </tbody>
        </table>
    </div>

    {% if quotation.terms %}
    <div class="terms">
        <h3>Conditions générales</h3>
        <p>{{ quotation.terms|nl2br }}</p>
    </div>
    {% endif %}

    <div class="footer">
        <p><strong>Service de Réservation de Salles</strong></p>
        <p>123 Rue de la Réservation, 75001 Paris | Tél: 01 23 45 67 89 | Email: contact@reservation.com</p>
        <p>Ce devis est valable jusqu'au {{ quotation.validUntil|date('d/m/Y') }}</p>
        <p style="margin-top: 20px;">
            <strong>Signature du client :</strong> _________________________________<br>
            <strong>Date :</strong> _________________________________
        </p>
    </div>
</body>
</html> 